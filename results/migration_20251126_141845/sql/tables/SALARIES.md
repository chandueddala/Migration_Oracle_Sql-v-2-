# SQL Server TABLE: SALARIES

**Source**: SQL Server (Converted)  
**Captured**: 2025-11-26 14:22:01  

## Source Code

```sql
CREATE TABLE [APP].[SALARIES] (
    [SALARY_ID] BIGINT IDENTITY(1,1) NOT NULL,
    [EMP_ID] DECIMAL(38,0) NOT NULL,
    [EFFECTIVE_FROM] DATETIME2 NOT NULL,
    [EFFECTIVE_TO] DATETIME2,
    [BASE_SALARY] DECIMAL(10,2) NOT NULL,
    [CURRENCY] NVARCHAR(3) NOT NULL DEFAULT 'USD',
    CONSTRAINT [PK_SALARIES] PRIMARY KEY ([SALARY_ID]),
    CONSTRAINT [FK_SAL_EMP] FOREIGN KEY ([EMP_ID]) REFERENCES [APP].[EMPLOYEES] ([EMP_ID])
);
```
