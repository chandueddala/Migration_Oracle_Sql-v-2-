# SQL Server TABLE: SALES_ORDER_ITEMS

**Source**: SQL Server (Converted)  
**Captured**: 2025-11-26 13:02:19  

## Source Code

```sql
CREATE TABLE [APP].[SALES_ORDER_ITEMS] (
    [ORDER_ITEM_ID] DECIMAL(38,0) IDENTITY(1,1) NOT NULL,
    [ORDER_ID] DECIMAL(38,0) NOT NULL,
    [PRODUCT_ID] DECIMAL(38,0) NOT NULL,
    [QTY] DECIMAL(10,0) NOT NULL,
    [UNIT_PRICE] DECIMAL(10,2) NOT NULL,
    [LINE_AMOUNT] AS ([QTY] * [UNIT_PRICE]) PERSISTED,
    CONSTRAINT [PK_SALES_ORDER_ITEMS] PRIMARY KEY ([ORDER_ITEM_ID]),
    CONSTRAINT [FK_SOI_ORDER] FOREIGN KEY ([ORDER_ID]) REFERENCES [APP].[SALES_ORDERS] ([ORDER_ID]),
    CONSTRAINT [FK_SOI_PROD] FOREIGN KEY ([PRODUCT_ID]) REFERENCES [APP].[PRODUCTS] ([PRODUCT_ID])
);
```
