# SQL Server TABLE: EMPLOYEES

**Source**: SQL Server (Converted)  
**Captured**: 2025-11-27 01:50:18  

## Source Code

```sql
CREATE TABLE [APP].[EMPLOYEES] (
    [EMP_ID] DECIMAL(38,0) IDENTITY(1,1) NOT NULL,
    [STORE_ID] DECIMAL(38,0) NOT NULL,
    [DEPT_ID] DECIMAL(38,0) NOT NULL,
    [FIRST_NAME] NVARCHAR(40) NOT NULL,
    [LAST_NAME] NVARCHAR(40) NOT NULL,
    [HIRE_DATE] DATETIME2 NOT NULL,
    [EMAIL] NVARCHAR(120),
    [STATUS] NVARCHAR(20) NOT NULL DEFAULT 'ACTIVE',
    CONSTRAINT [PK_EMPLOYEES] PRIMARY KEY ([EMP_ID]),
    CONSTRAINT [UQ_EMPLOYEES_EMAIL] UNIQUE ([EMAIL])
);
```
