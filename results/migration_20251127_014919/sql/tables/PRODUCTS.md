# SQL Server TABLE: PRODUCTS

**Source**: SQL Server (Converted)  
**Captured**: 2025-11-27 01:50:39  

## Source Code

```sql
CREATE TABLE [APP].[PRODUCTS] (
    [PRODUCT_ID] DECIMAL(38,0) IDENTITY(1,1) NOT NULL,
    [CATEGORY_ID] DECIMAL(38,0) NOT NULL,
    [SKU] NVARCHAR(30) NOT NULL,
    [PRODUCT_NAME] NVARCHAR(120) NOT NULL,
    [UNIT_PRICE] DECIMAL(10,2) NOT NULL,
    [ACTIVE] NCHAR(1) NOT NULL DEFAULT 'Y',
    CONSTRAINT [PK_PRODUCTS] PRIMARY KEY ([PRODUCT_ID]),
    CONSTRAINT [UQ_PRODUCTS_SKU] UNIQUE ([SKU])
);
```
