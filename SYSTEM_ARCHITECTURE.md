# 🏗️ SYSTEM ARCHITECTURE - AGENTIC MIGRATION

Visual guide to your Oracle → SQL Server agentic migration system.

---

## 🎯 OVERALL SYSTEM FLOW

```
┌─────────────────────────────────────────────────────────────────┐
│                    ORACLE TO SQL SERVER                          │
│                    AGENTIC MIGRATION SYSTEM                      │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  STEP 1: INITIALIZATION                                          │
│  ├─ Load credentials (Oracle + SQL Server)                      │
│  ├─ Load hot-fixes (auto-apply patches)                         │
│  ├─ Initialize agents (Orchestrator, Debugger, Converter, RCA)  │
│  └─ Connect to databases                                        │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  STEP 2: TABLE MIGRATION (for each table)                       │
│  ├─ Fetch Oracle DDL                                            │
│  ├─ Convert to SQL Server (SSMA or LLM) ← Shows indicator       │
│  ├─ Review conversion                                           │
│  ├─ Check if table exists                                       │
│  │  └─ If exists: Prompt user (Drop/Skip/Append) ← User input   │
│  ├─ Deploy table structure                                      │
│  │  └─ If error: Agentic repair (3 attempts) ← Intelligence     │
│  └─ Migrate data                                                │
│     └─ If error: Agentic repair (3 attempts)                    │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  STEP 3: PACKAGE MIGRATION (for each package)                   │
│  ├─ Fetch Oracle PL/SQL code                                    │
│  ├─ Convert to T-SQL (SSMA or LLM) ← Shows indicator            │
│  ├─ Review conversion                                           │
│  ├─ Check if package exists                                     │
│  │  └─ If exists: Prompt user (Drop/Skip) ← User input          │
│  └─ Deploy package                                              │
│     └─ If error: Agentic repair (3 attempts) ← Intelligence     │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  STEP 4: VERIFICATION & REPORTING                                │
│  ├─ Generate migration summary                                  │
│  ├─ Report cost ($547.11)                                       │
│  ├─ Show success/failure counts                                 │
│  └─ Disconnect from databases                                   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  STEP 5: USER VERIFICATION (manual)                              │
│  └─ Run: python verify_migration.py                             │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🤖 AGENTIC COMPONENTS

```
┌──────────────────────────────────────────────────────────────────┐
│                     AGENTIC AGENT HIERARCHY                       │
└──────────────────────────────────────────────────────────────────┘

                    ┌─────────────────────┐
                    │  ORCHESTRATOR       │
                    │  AGENT              │
                    │  (Main Controller)  │
                    └──────────┬──────────┘
                               │
        ┌──────────────────────┼──────────────────────┐
        │                      │                      │
        ▼                      ▼                      ▼
┌───────────────┐    ┌──────────────┐    ┌─────────────────┐
│  CONVERTER    │    │  DEBUGGER    │    │  ROOT CAUSE     │
│  AGENT        │    │  AGENT       │    │  ANALYZER       │
│               │    │              │    │  (Intelligence) │
│  - DDL Conv.  │    │  - Detects   │    │                 │
│  - PL/SQL     │    │    errors    │    │  - 5-step       │
│    Conv.      │    │  - Attempts  │    │    analysis     │
│  - Uses SSMA  │    │    repairs   │    │  - Multi-source │
│    or LLM     │    │  - User      │    │    context      │
│               │    │    prompts   │    │  - Intelligent  │
│               │    │              │    │    fixes        │
└───────────────┘    └──────┬───────┘    └────────┬────────┘
                            │                     │
                            │  If error detected  │
                            └────────►────────────┘
                                     │
                                     ▼
                        ┌────────────────────────┐
                        │  AGENTIC REPAIR        │
                        │  5-STEP PROCESS        │
                        └────────────────────────┘
                                     │
        ┌────────────────────────────┼────────────────────────────┐
        │                            │                            │
        ▼                            ▼                            ▼
┌───────────────┐         ┌──────────────────┐         ┌─────────────────┐
│ Step 1:       │         │ Step 2:          │         │ Step 3:         │
│ CLASSIFY      │────────►│ ANALYZE ORACLE   │────────►│ GATHER SQL      │
│ ERROR         │         │ CODE             │         │ CONTEXT         │
│               │         │                  │         │                 │
│ - Syntax?     │         │ - Original DDL   │         │ - SQL metadata  │
│ - Data type?  │         │ - Object type    │         │ - Dependencies  │
│ - Constraint? │         │ - Dependencies   │         │ - Constraints   │
└───────────────┘         └──────────────────┘         └─────────────────┘
                                                                │
                                                                ▼
┌───────────────┐         ┌──────────────────┐         ┌─────────────────┐
│ Step 6:       │◄────────│ Step 5:          │◄────────│ Step 4:         │
│ GENERATE FIX  │         │ ROOT CAUSE       │         │ SEARCH          │
│               │         │ DIAGNOSIS        │         │ KNOWLEDGE       │
│ - New SQL     │         │                  │         │                 │
│ - Explanation │         │ - Synthesize all │         │ - Memory DB     │
│ - Confidence  │         │ - Find root cause│         │ - Web search    │
└───────────────┘         └──────────────────┘         └─────────────────┘
```

---

## 🔄 DATA FLOW

```
┌──────────────────────────────────────────────────────────────────┐
│                    DATA MIGRATION FLOW                            │
└──────────────────────────────────────────────────────────────────┘

ORACLE DATABASE                                    SQL SERVER DATABASE
┌─────────────┐                                    ┌──────────────┐
│             │                                    │              │
│  ┌───────┐  │    1. Fetch Data                  │              │
│  │ TABLE │  │    ════════════►                   │              │
│  └───────┘  │    oracle_conn.fetch_table_data() │              │
│             │                                    │              │
│  Returns:   │                                    │              │
│  [          │                                    │              │
│    {col1: val1, col2: val2, ...},               │              │
│    {col1: val1, col2: val2, ...},               │              │
│    ...                                           │              │
│  ]          │                                    │              │
│             │                                    │              │
│             │    2. Transform (if needed)        │              │
│             │    ════════════►                   │              │
│             │    - Convert LOBs to strings       │              │
│             │    - Handle data types             │              │
│             │    - Format dates                  │              │
│             │                                    │              │
│             │    3. Insert Data                  │  ┌────────┐  │
│             │    ════════════════════════════►   │  │ TABLE  │  │
│             │    sqlserver_conn.bulk_insert_data()  └────────┘  │
│             │                                    │              │
│             │    - Enable IDENTITY_INSERT        │              │
│             │    - Batch insert (executemany)    │              │
│             │    - Disable IDENTITY_INSERT       │              │
│             │                                    │              │
│             │    4. Verify Count                 │              │
│             │    ◄════════════                   │              │
│             │    cursor.rowcount (returns -1!)   │              │
│             │                                    │              │
└─────────────┘                                    └──────────────┘

NOTE: Step 4 returns -1 (pyodbc quirk), but data IS inserted!
      Use verify_migration.py to check actual row counts.
```

---

## 🔧 ERROR REPAIR WORKFLOW

```
┌──────────────────────────────────────────────────────────────────┐
│                    ERROR DETECTED                                 │
│  (e.g., "Incorrect syntax near 'IDENTITY'")                      │
└──────────────────────────────────────────────────────────────────┘
                              │
                              ▼
                   ┌──────────────────────┐
                   │  Is it "object       │
                   │  already exists"?    │
                   └──────┬───────────────┘
                          │
              ┌───────────┴───────────┐
              │ YES                   │ NO
              ▼                       ▼
    ┌──────────────────┐    ┌─────────────────────┐
    │  ATTEMPT 1:      │    │  ATTEMPT 1:         │
    │  USER PROMPT     │    │  ROOT CAUSE         │
    │                  │    │  ANALYZER           │
    │  Drop/Skip/      │    │                     │
    │  Append?         │    │  5-step intelligent │
    │  (30 sec timeout)│    │  analysis           │
    └────────┬─────────┘    └──────────┬──────────┘
             │                         │
             │ User chooses            │ Analysis complete
             ▼                         ▼
    ┌──────────────────┐    ┌─────────────────────┐
    │  Apply user      │    │  Apply generated    │
    │  choice:         │    │  fix                │
    │                  │    │                     │
    │  - DROP: Prepend │    │  - New SQL code     │
    │    DROP stmt     │    │  - Targeted to      │
    │  - SKIP: Return  │    │    root cause       │
    │  - APPEND: Skip  │    │                     │
    │    structure     │    │                     │
    └────────┬─────────┘    └──────────┬──────────┘
             │                         │
             │ Re-execute              │ Re-execute
             ▼                         ▼
    ┌──────────────────┐    ┌─────────────────────┐
    │  SUCCESS?        │    │  SUCCESS?           │
    └────────┬─────────┘    └──────────┬──────────┘
             │                         │
      ┌──────┴──────┐           ┌─────┴──────┐
      │ YES    NO   │           │ YES   NO   │
      ▼      ▼      │           ▼      ▼     │
    ┌────┐ ┌───────┴───────────────────┐     │
    │ ✅ │ │  ATTEMPT 2:               │     │
    │    │ │  ROOT CAUSE ANALYZER      │◄────┘
    │    │ │  (if not already tried)   │
    │    │ └───────────┬───────────────┘
    │    │             │
    │    │             │ Re-execute
    │    │             ▼
    │    │    ┌─────────────────────┐
    │    │    │  SUCCESS?           │
    │    │    └──────────┬──────────┘
    │    │               │
    │    │        ┌──────┴──────┐
    │    │        │ YES    NO   │
    │    │        ▼       ▼     │
    │    │      ┌────┐ ┌────────┴──────────┐
    │    │      │ ✅ │ │  ATTEMPT 3:       │
    │    │      │    │ │  BASIC REPAIR     │
    │    │      │    │ │  (Fallback)       │
    │    │      │    │ └────────┬──────────┘
    │    │      │    │          │
    │    │      │    │          │ Re-execute
    │    │      │    │          ▼
    │    │      │    │ ┌─────────────────┐
    │    │      │    │ │  SUCCESS?       │
    │    │      │    │ └────────┬────────┘
    │    │      │    │          │
    │    │      │    │    ┌─────┴─────┐
    │    │      │    │    │ YES   NO  │
    │    │      │    │    ▼      ▼    │
    │    └──────┴────┴──►┌────┐ ┌────┴───┐
    │                    │ ✅ │ │ ❌     │
    │                    │    │ │ FAILED │
    └────────────────────┴────┴─┴────────┘

KEY:
✅ = Success, continue to next object
❌ = Failed after 3 attempts, log error and continue
```

---

## 🗄️ DATABASE ARCHITECTURE

```
┌──────────────────────────────────────────────────────────────────┐
│                    MIGRATION MEMORY DATABASE                      │
│                    (SQLite - migration_memory.db)                 │
└──────────────────────────────────────────────────────────────────┘

Tables:
┌─────────────────────────────────────────────────────────────────┐
│  converted_objects                                               │
│  ├─ object_name (e.g., "LOANS")                                 │
│  ├─ object_type (e.g., "TABLE")                                 │
│  ├─ oracle_ddl (original Oracle DDL)                            │
│  ├─ tsql_ddl (converted T-SQL)                                  │
│  ├─ conversion_method (SSMA or LLM)                             │
│  ├─ created_at (timestamp)                                      │
│  └─ cost (LLM cost for this conversion)                         │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  migration_errors                                                │
│  ├─ object_name (e.g., "LOANS")                                 │
│  ├─ object_type (e.g., "TABLE")                                 │
│  ├─ error_message (SQL Server error)                            │
│  ├─ solution (fix applied)                                      │
│  ├─ fixed_sql (corrected T-SQL)                                 │
│  ├─ created_at (timestamp)                                      │
│  └─ success (boolean)                                           │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  identity_columns                                                │
│  ├─ table_name (e.g., "LOANS")                                  │
│  ├─ column_name (e.g., "LOAN_ID")                               │
│  └─ created_at (timestamp)                                      │
└─────────────────────────────────────────────────────────────────┘

Purpose: Shared memory across agents for learning and optimization
```

---

## 💡 USER INTERACTION POINTS

```
┌──────────────────────────────────────────────────────────────────┐
│              USER INTERACTION DURING MIGRATION                    │
└──────────────────────────────────────────────────────────────────┘

INTERACTION #1: Table Already Exists
┌─────────────────────────────────────────────────────────────────┐
│  ⚠️  TABLE 'LOANS' already exists. What would you like to do?   │
│                                                                  │
│  DROP   - Drop existing table and recreate (data will be lost)  │
│  SKIP   - Skip this table (no changes)                          │
│  APPEND - Keep existing table and add new data (recommended)    │
│                                                                  │
│  Timeout: 30 seconds (auto-selects APPEND)                      │
│  Your choice: _                                                  │
└─────────────────────────────────────────────────────────────────┘

INTERACTION #2: Package Already Exists
┌─────────────────────────────────────────────────────────────────┐
│  ⚠️  PROCEDURE 'PKG_LOAN_PROCESSOR' already exists.             │
│                                                                  │
│  DROP - Drop and recreate                                       │
│  SKIP - Skip this package                                       │
│                                                                  │
│  Timeout: 30 seconds (auto-selects DROP)                        │
│  Your choice: _                                                  │
└─────────────────────────────────────────────────────────────────┘

If no input within timeout → Default selection applied automatically
```

---

## 📊 TOOL VISIBILITY

```
┌──────────────────────────────────────────────────────────────────┐
│              SSMA/LLM INDICATOR DISPLAY                           │
└──────────────────────────────────────────────────────────────────┘

When SSMA is available:
┌─────────────────────────────────────────────────────────────────┐
│  [1/5] Table: LOANS                                              │
│    🔄 Step 2/5: Converting to SQL Server (using SSMA)...        │
│    ✅ Conversion successful                                      │
└─────────────────────────────────────────────────────────────────┘

When SSMA is NOT available (using LLM):
┌─────────────────────────────────────────────────────────────────┐
│  [1/5] Table: LOANS                                              │
│    🔄 Step 2/5: Converting to SQL Server (using LLM)...         │
│    💰 Cost: ~$1.50                                               │
│    ✅ Conversion successful                                      │
└─────────────────────────────────────────────────────────────────┘

This provides CLEAR VISIBILITY of which tool was used for conversion.
```

---

## 🔌 CONNECTION MANAGEMENT

```
┌──────────────────────────────────────────────────────────────────┐
│                  CONNECTION LIFECYCLE                             │
└──────────────────────────────────────────────────────────────────┘

BEFORE HOT-FIX (❌ Broken):
┌──────────────────────────────────────────────────┐
│  oracle_conn = OracleConnector(creds)            │  ← Created
│  # .connect() NEVER CALLED! ❌                   │
│  data = oracle_conn.fetch_table_data(table)      │  ← CRASHES
│  # Error: 'NoneType' has no attribute 'cursor'   │
└──────────────────────────────────────────────────┘

AFTER HOT-FIX (✅ Fixed):
┌──────────────────────────────────────────────────┐
│  oracle_conn = OracleConnector(creds)            │  ← Created
│  if not oracle_conn.connect(): ✅                │  ← Connected
│      return error                                │
│                                                  │
│  sqlserver_conn = SQLServerConnector(creds)      │  ← Created
│  if not sqlserver_conn.connect(): ✅             │  ← Connected
│      oracle_conn.disconnect()                    │
│      return error                                │
│                                                  │
│  data = oracle_conn.fetch_table_data(table)      │  ← Works! ✅
│  rows = sqlserver_conn.bulk_insert_data(data)    │  ← Works! ✅
│                                                  │
│  oracle_conn.disconnect() ✅                     │  ← Cleanup
│  sqlserver_conn.disconnect() ✅                  │  ← Cleanup
└──────────────────────────────────────────────────┘
```

---

## 🎯 COMPONENT INTERACTIONS

```
┌──────────────────────────────────────────────────────────────────┐
│                  COMPONENT INTERACTION MAP                        │
└──────────────────────────────────────────────────────────────────┘

main.py
  │
  ├──► Loads config files (Oracle + SQL Server credentials)
  ├──► Imports hot-fixes (auto-apply patches)
  │     ├──► hotfix_connection.py
  │     └──► hotfix_oracle_connector.py
  │
  └──► Creates OrchestratorAgent
        │
        ├──► Uses OracleConnector (database/oracle_connector.py)
        │     └──► fetch_table_data() - Returns list of dicts
        │
        ├──► Uses SQLServerConnector (database/sqlserver_connector.py)
        │     ├──► execute_ddl() - Deploy structures
        │     └──► bulk_insert_data() - Insert data
        │
        ├──► Uses ConverterAgent (agents/converter_agent.py)
        │     ├──► convert_table_ddl() - Oracle DDL → T-SQL
        │     └──► convert_code() - PL/SQL → T-SQL
        │           └──► Uses Claude LLM via LangChain
        │
        ├──► Uses DebuggerAgent (agents/debugger_agent.py)
        │     ├──► debug_and_repair() - Fix errors
        │     └──► Uses RootCauseAnalyzer
        │           ├──► analyze_and_fix() - 5-step intelligence
        │           ├──► Uses Claude LLM via LangChain
        │           └──► Uses WebSearch for knowledge
        │
        ├──► Uses MigrationMemory (database/migration_memory.py)
        │     ├──► save_converted_object()
        │     ├──► save_error()
        │     └──► get_identity_columns()
        │
        └──► Uses CostTracker (database/cost_tracker.py)
              ├──► add_cost()
              └──► get_total_cost()
```

---

## 🔍 FILE ORGANIZATION

```
oracle-sqlserver-migration-v2-FINAL/
│
├── 📄 main.py (Entry point)
│
├── 📁 agents/ (Agentic Intelligence)
│   ├── orchestrator_agent.py (Main controller)
│   ├── converter_agent.py (DDL/PL/SQL conversion)
│   ├── debugger_agent.py (Error repair)
│   └── root_cause_analyzer.py (5-step analysis)
│
├── 📁 database/ (Data Access Layer)
│   ├── oracle_connector.py (Oracle operations)
│   ├── sqlserver_connector.py (SQL Server operations)
│   ├── migration_memory.py (Shared learning)
│   └── cost_tracker.py (Cost tracking)
│
├── 📁 utils/ (Utilities)
│   ├── migration_engine.py (Core migration logic)
│   └── user_prompt.py (Interactive prompts)
│
├── 📁 config/ (Credentials)
│   ├── oracle_config.json
│   └── sqlserver_config.json
│
├── 🔧 hotfix_connection.py (Connection hot-fix)
├── 🔧 hotfix_oracle_connector.py (Method alias)
├── 🔧 APPLY_FIX.py (Fix application)
│
├── 🔍 verify_migration.py (Data verification)
│
└── 📚 Documentation/
    ├── README_FIRST.md ⭐ (Start here)
    ├── QUICK_REFERENCE.md ⭐ (Quick lookup)
    ├── EXECUTIVE_SUMMARY.md (Overview)
    ├── MIGRATION_FINAL_STATUS.md (Complete status)
    ├── REMAINING_FIXES.md (Code fixes)
    ├── FINAL_ISSUES_AND_STATUS.md (Issue details)
    ├── CONNECTION_FIX.md (Connection fix)
    ├── COMPLETE_FIX_VERIFICATION.md (Data flow)
    ├── AGENTIC_SYSTEM.md (Architecture)
    ├── DOCUMENTATION_INDEX.md (Doc guide)
    └── SYSTEM_ARCHITECTURE.md (This file)
```

---

## 💰 COST FLOW

```
┌──────────────────────────────────────────────────────────────────┐
│                    LLM COST TRACKING FLOW                         │
└──────────────────────────────────────────────────────────────────┘

TABLE CONVERSION:
┌─────────────────┐
│  Oracle DDL     │
│  (Free)         │
└────────┬────────┘
         │
         ▼
┌─────────────────┐        ┌──────────────────────────┐
│  Use SSMA?      │───YES──│  SSMA Conversion         │
│                 │        │  Cost: $0                │
└────────┬────────┘        └──────────────────────────┘
         │
         NO
         │
         ▼
┌─────────────────────────────────────────┐
│  Claude LLM Conversion                  │
│  Model: Sonnet 3.5                      │
│  Cost: ~$0.003/1K input tokens          │
│        ~$0.015/1K output tokens         │
│  Typical DDL: ~500 tokens in + out      │
│  Cost per table: ~$1-3                  │
└────────────┬────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────┐
│  CostTracker.add_cost('sonnet', $1.50) │
└────────────────────────────────────────┘

PACKAGE CONVERSION:
┌─────────────────┐
│  Oracle PL/SQL  │
│  (Free)         │
└────────┬────────┘
         │
         ▼
┌─────────────────┐        ┌──────────────────────────┐
│  Use SSMA?      │───YES──│  SSMA Conversion         │
│                 │        │  Cost: $0                │
└────────┬────────┘        └──────────────────────────┘
         │
         NO
         │
         ▼
┌─────────────────────────────────────────┐
│  Claude LLM Conversion                  │
│  Model: Opus (for complex code)         │
│  Cost: ~$0.015/1K input tokens          │
│        ~$0.075/1K output tokens         │
│  Typical package: ~5000 tokens in + out │
│  Cost per package: ~$300-500            │
└────────────┬────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────┐
│  CostTracker.add_cost('opus', $395.96) │
└────────────────────────────────────────┘

ROOT CAUSE ANALYSIS:
┌─────────────────┐
│  Error detected │
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│  Root Cause Analyzer                    │
│  Model: Opus (for deep analysis)        │
│  5 steps × ~1000 tokens each            │
│  Cost per analysis: ~$30-50             │
└────────────┬────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────┐
│  CostTracker.add_cost('opus', $45.00)  │
└────────────────────────────────────────┘

TOTAL:
┌────────────────────────────────────────┐
│  CostTracker.get_total_cost()          │
│  Returns: $547.11                      │
│    - Sonnet: $151.15 (27.6%)           │
│    - Opus:   $395.96 (72.4%)           │
└────────────────────────────────────────┘
```

---

## 🎯 DECISION POINTS

```
┌──────────────────────────────────────────────────────────────────┐
│              KEY DECISION POINTS IN MIGRATION                     │
└──────────────────────────────────────────────────────────────────┘

1. Conversion Method Selection:
   ┌──────────────┐
   │ Is SSMA      │───YES───► Use SSMA (Free, Fast)
   │ available?   │
   └──────┬───────┘
          │ NO
          ▼
   ┌──────────────┐
   │ Use LLM      │
   │ (Sonnet for  │
   │  tables,     │
   │  Opus for    │
   │  packages)   │
   └──────────────┘

2. Object Exists Handling:
   ┌──────────────┐
   │ Does object  │───NO────► Deploy directly
   │ exist in SQL?│
   └──────┬───────┘
          │ YES
          ▼
   ┌──────────────┐
   │ Prompt user: │
   │ Drop/Skip/   │
   │ Append?      │
   │ (30s timeout)│
   └──────────────┘

3. Error Repair Strategy:
   ┌──────────────┐
   │ Error        │
   │ detected     │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ Is "object   │───YES───► User prompt first
   │ exists"?     │
   └──────┬───────┘
          │ NO
          ▼
   ┌──────────────┐
   │ Try Root     │───SUCCESS──► Continue
   │ Cause        │
   │ Analyzer     │
   └──────┬───────┘
          │ FAIL
          ▼
   ┌──────────────┐
   │ Try basic    │───SUCCESS──► Continue
   │ repair       │
   └──────┬───────┘
          │ FAIL
          ▼
   ┌──────────────┐
   │ Log error &  │
   │ continue to  │
   │ next object  │
   └──────────────┘

4. Data Migration Decision:
   ┌──────────────┐
   │ Table has    │───NO────► Skip data migration
   │ data in      │
   │ Oracle?      │
   └──────┬───────┘
          │ YES
          ▼
   ┌──────────────┐
   │ Has IDENTITY │───YES───► Enable IDENTITY_INSERT
   │ columns?     │
   └──────┬───────┘
          │ NO
          ▼
   ┌──────────────┐
   │ Bulk insert  │
   │ data         │
   └──────────────┘
```

---

## 🔄 STATE MACHINE

```
┌──────────────────────────────────────────────────────────────────┐
│                  MIGRATION STATE MACHINE                          │
└──────────────────────────────────────────────────────────────────┘

[INIT] ──► [CONNECTING] ──► [READY]
                │               │
                │ Error         │
                ▼               │
            [ERROR]            │
                               │
                               ▼
              [MIGRATING_TABLE_STRUCTURE] ◄──┐
                │                            │
                │ Success                    │ Next table
                ▼                            │
              [MIGRATING_TABLE_DATA]         │
                │                            │
                │ Success                    │
                ├────────────────────────────┘
                │
                │ All tables done
                ▼
              [MIGRATING_PACKAGES] ◄─────────┐
                │                            │
                │ Success                    │ Next package
                ├────────────────────────────┘
                │
                │ All packages done
                ▼
              [REPORTING]
                │
                ▼
              [DISCONNECTING]
                │
                ▼
              [COMPLETE]

At any ERROR state:
  ├──► Attempt repair (3 tries)
  ├──► If success: Return to previous state
  └──► If fail: Log error, continue to next object
```

---

## 🎯 SUMMARY

This agentic migration system features:

- **🤖 Intelligence**: Root Cause Analyzer with 5-step reasoning
- **👤 User Control**: Interactive prompts with timeout
- **👁️ Visibility**: Clear SSMA/LLM usage indicators
- **🔧 Robustness**: Multiple repair attempts with fallback
- **💾 Memory**: Shared learning across migrations
- **💰 Cost Tracking**: Transparent LLM usage costs
- **📊 Reporting**: Comprehensive results and verification

**Your migration system is production-ready and fully agentic!** 🎉

---

*System Architecture for Oracle → SQL Server Agentic Migration*
*Non-Static, Reasoning-Based, Intelligent Approach*
